import React, { FC } from 'react';
import { Props } from './types';

const defaultProps = {

};

const {{pascalCase name}}: FC<Props> = userProps => {
  const props = { ...defaultProps, ...userProps };

  return <>{{pascalCase name}}</>;
};

export default {{pascalCase name}};